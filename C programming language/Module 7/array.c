// datatype name[size]
// ------ convension ------
#include<stdio.h>
int main()
{
    int arr[10]; // creating an array
    // indexing --- 0 to 9
    // --- Accessing an Array ---
    // 1 index forward means adding 4 every time
    // so arr[3]=410+3*4, as arr[0]=410 #memoryLocation
    // so in 422 #mL, the value will be given
    // --------------------------
    return 0;
}
