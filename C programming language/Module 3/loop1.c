#include <stdio.h>
int main()
{
    int i;
    //for(initialization; condition; increment or decrement)
    for(i=1;i<=3;i=i+1)   // i+=1, i++
    {
        printf("Hello\n");
    }
    return 0;
}