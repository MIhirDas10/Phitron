#include<stdio.h>
#include<math.h>
int main()
{
    double x;
    scanf("%lf",&x);
    double ans=sqrt(x); // in order to recieve the floating values
    printf("%lf",ans);
    // printf("%.2lf",ans);
    return 0;
}

// root over of 25 = 5
// root over of 21 = 4.582576
// root over of 81 = 9